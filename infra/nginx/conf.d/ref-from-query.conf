# Sempre setar bf_ref quando vier ?ref=VAL (validação 6–12 A-Z/0-9)
map "$arg_ref" $bf_ref_cookie_val {
    default "";
    "~^([A-Za-z0-9]{6,12})$"  "bf_ref=$1; Max-Age=2592000; Domain=.betforbes.com; Path=/; HttpOnly; SameSite=Lax; Secure";
}
